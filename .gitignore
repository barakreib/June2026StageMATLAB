# Generated per-day stimulus manifests — DATA, not code (they live beside the .abf files).
*_stim_manifest.jsonl
*_stim_manifest.json
*_stim_manifest.json.tmp

# macOS / editor junk
.DS_Store

# machine-local Claude config — never commit
.claude/
timing_tests/

# per-run generated stimulus scripts (written next to the day's data at Run time)
generated_stimuli/
*_values.csv

# lcr4500-linearize: local python env + bytecode (recreate via setup.bat / requirements.txt)
lcr4500-linearize/.venv/
__pycache__/
